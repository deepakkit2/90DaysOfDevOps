"Everything in Linux is either a file or a directory" I "Everything is a process"
Dockerfile se bannti hai Image, image se bannta hai container"

docker structure

  
dockers 

Docker Concepts

• Why, what, and how do we learn Docker?
• Virtualization vs Containerisation
• Terminologies - Docker Engine, Docker Client, Docker Daemon, Docker Registry, Docker
CLI, Docker, docker hub

• Containers — Nginx, MySQL, MongoDB, ubuntu

Docker Advanced: 2-3 years Experience
• My Docker image size is too big. reduce it and secure it [Multi-stage Docker Builds]
(Distroless images) [l feel confident in this scenario]l
1
• My container crashed; I lost all my data. How do I persist my data? [Docker Volumes]
• I want to run a multi-tier project which has a frontend, a backend, database, and Al LLM
•
•
•
integration [Docker Compose — Docker Network, Docker Volumes, Health Checks].
Docker Hardened Images
Docker Scout
Docker Hub



docker cmds
docker ps
docker run
docker build —t flask—app .   // build and image

docker pull python31.4-slim  // pull imgae from offical repo to local
docker images // to show local images

docker build —f Docker file.multisltage —t flask—app—mini .  

docker volume create mysql—data mysql—data //create volumne

